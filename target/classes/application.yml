spring:
  datasource:
    url: jdbc:mysql://localhost:3306/xianHua?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: sdx121380303.
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 10000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
  #熔断器和限流配置文件
  resilience4j:
    ratelimiter:
      instances:
        appointment-limiter:
          limit-for-period: 10        # 每秒10个请求
          limit-refresh-period: 1s

    circuitbreaker:
      instances:
        appointment-breaker:
          failure-rate-threshold: 50   # 失败率50%触发熔断
          wait-duration-in-open-state: 10s  # 熔断10秒



  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin123
    virtual-host: /
    # 连接池配置
    connection-timeout: 30s
    # 开启消息确认
    publisher-confirm-type: correlated
    publisher-returns: true



  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
ai:
  service:
    url: http://localhost:8001
    chat:
      endpoint: /api/ai/chat
      connect-timeout: 30000
      read-timeout: 30000
redisson:
  config: |
    singleServerConfig:
      address: "redis://localhost:6379"
  # 微信配置类（注意：两个空格缩进，放在 spring 下面）
wechat:
     appid: wx7eda19d6937d9ea8
     secret: 0ce89bf5dd7c5adfaec82dfacea6b536

server:
  port: 8080
  address: 0.0.0.0

sa-token:
  token-name: satoken
  timeout: 86400
  is-concurrent: true
  is-share: false
  token-style: uuid
  is-log: true


mybatis:
  mapper-locations: classpath:mapper/**/*.xml  # 改成两个星号，支持子目录
  type-aliases-package: com.explore.xianhuaback.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


qiNiu:
  access-key: QjDZWYkcGZdT0_BFtd2uRNg6os-AbmbtT3vHigFc
  secret-key: bZr5895gvX_eE6V-AkPkS74REkZbHEnAFNS5maoQ
  bucket: productsflows
  domain: http://tbvyb9pvp.hd-bkt.clouddn.com